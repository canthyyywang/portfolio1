export const metadata = {
  title: "AI 开放平台系统级体验重塑 · 王启萌",
  description: "底层架构、数据驱动、B端体验 — 项目案例",
};

export default function ProjectAiPlatformLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
}
